<target name ="clean">
	<delete dir="${build}"/>
</taregt>

<target name = "init" depends="clean">
	<mkdir dir "${build}/classes"/>
</target>

<target name="compile" depends="init"/>
	<javac includeruntime ="false" srcdir = "$src" destdir = "${build}/classes/>
</target>

<target name = "run" depends="compile"/>
	<java classname = 
</target>
